name: Agent MR Review
on:
  pull_request:


jobs:
  agent_job:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write # Required to post comments
      id-token: write      # Required for Cloud Run Auth
    
    steps:
